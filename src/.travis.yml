language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t daisenthal/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true daisenthal/docker-react npm run test
